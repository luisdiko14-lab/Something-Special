const c=document.getElementById('game'),x=c.getContext('2d');const scoreEl=document.getElementById('score'),bestEl=document.getElementById('best'),levelEl=document.getElementById('level'),shieldEl=document.getElementById('shield'),btn=document.getElementById('start');let keys={},running=false,score=0,best=+localStorage.neonBest||0,level=1,shield=3,player,gem,hazards,last=0,spawn=0;
bestEl.textContent=best;addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(['arrowup','arrowdown','arrowleft','arrowright',' '].includes(e.key.toLowerCase()))e.preventDefault()});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);btn.onclick=start;
function start(){running=true;score=0;level=1;shield=3;player={x:450,y:260,r:14};gem=makeGem();hazards=[];last=performance.now();btn.textContent='RESTART';requestAnimationFrame(loop)}function makeGem(){return{x:45+Math.random()*810,y:45+Math.random()*430,r:9}}function addHazard(){const side=Math.floor(Math.random()*4);let h={x:side<2?Math.random()*900:(side===2?-20:920),y:side<2?(side===0?-20:540):Math.random()*520,vx:0,vy:0,r:12};const dx=player.x-h.x,dy=player.y-h.y,d=Math.hypot(dx,dy)||1,s=70+level*13;h.vx=dx/d*s;h.vy=dy/d*s;hazards.push(h)}
function loop(t){if(!running)return;const dt=Math.min((t-last)/1000,.035);last=t;update(dt);draw();requestAnimationFrame(loop)}function update(dt){let dx=(keys.d||keys.arrowright?1:0)-(keys.a||keys.arrowleft?1:0),dy=(keys.s||keys.arrowdown?1:0)-(keys.w||keys.arrowup?1:0);const m=Math.hypot(dx,dy)||1;player.x=Math.max(20,Math.min(880,player.x+dx/m*260*dt));player.y=Math.max(20,Math.min(500,player.y+dy/m*260*dt));if(Math.hypot(player.x-gem.x,player.y-gem.y)<player.r+gem.r){score+=10;level=1+Math.floor(score/80);gem=makeGem();scoreEl.textContent=score;levelEl.textContent=level}spawn+=dt;if(spawn>Math.max(.35,1.15-level*.06)){spawn=0;addHazard()}hazards.forEach(h=>{h.x+=h.vx*dt;h.y+=h.vy*dt});for(let i=hazards.length-1;i>=0;i--){const h=hazards[i];if(Math.hypot(player.x-h.x,player.y-h.y)<player.r+h.r){hazards.splice(i,1);shield--;shieldEl.textContent='●'.repeat(shield)+'○'.repeat(3-shield);if(shield<=0)gameOver()}}}
function gameOver(){running=false;if(score>best){best=score;localStorage.neonBest=best;bestEl.textContent=best}btn.textContent='PLAY AGAIN'}function draw(){x.clearRect(0,0,c.width,c.height);x.fillStyle='#090c18';x.fillRect(0,0,c.width,c.height);x.strokeStyle='#151a2d';x.lineWidth=1;for(let i=0;i<c.width;i+=45){x.beginPath();x.moveTo(i,0);x.lineTo(i,c.height);x.stroke()}for(let j=0;j<c.height;j+=45){x.beginPath();x.moveTo(0,j);x.lineTo(c.width,j);x.stroke()}x.shadowBlur=18;x.shadowColor='#fff';x.fillStyle='#fff';x.beginPath();x.arc(player.x,player.y,player.r,0,Math.PI*2);x.fill();x.shadowColor='#8cf';x.fillStyle='#8cf';x.save();x.translate(gem.x,gem.y);x.rotate(performance.now()/500);x.beginPath();x.moveTo(0,-gem.r);x.lineTo(gem.r,0);x.lineTo(0,gem.r);x.lineTo(-gem.r,0);x.closePath();x.fill();x.restore();x.shadowColor='#f55';x.fillStyle='#f55';hazards.forEach(h=>{x.beginPath();x.arc(h.x,h.y,h.r,0,Math.PI*2);x.fill()});x.shadowBlur=0;if(!running){x.fillStyle='rgba(4,5,12,.72)';x.fillRect(0,0,c.width,c.height);x.fillStyle='#fff';x.textAlign='center';x.font='800 42px system-ui';x.fillText(score?'RUN OVER':'NEON DASH',450,245);x.font='16px system-ui';x.fillStyle='#aeb5d2';x.fillText(score?`Score: ${score}  ·  Press PLAY AGAIN`:'Press START RUN',450,280)}}
